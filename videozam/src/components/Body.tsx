import MainContainer from "./MainContainer";
import Sidebar from "./Sidebar";

const Body = () => {
  return (
    <main>
      <Sidebar />
      <MainContainer />
    </main>
  );
};

export default Body;
